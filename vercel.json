{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install -g pnpm && pnpm install --prefer-offline",
        "buildCommand": "pnpm run build:vercel"
      }
    }
  ]
}